# MIDI Core quality gates

Status: mandatory acceptance contract

Authority: evidence required to advance or complete a phase

## 1. Principles

- Structural correctness is necessary but does not prove musical usefulness.
- Musical preference does not excuse corrupted MIDI, authority drift, or unsafe
  persistence.
- Every gate binds evidence to fixture/source and generated artifact hashes.
- A failed gate produces a targeted correction; it does not authorize a broad
  rewrite or unrestricted model pass.
- Optional AI and pad work cannot weaken deterministic MVP gates.

## 2. Documentation gate

Before implementation tasks begin:

- root `PLAN.md` is the only active roadmap;
- target architecture, functions, MIDI contract, DAW boundary, quality gates,
  and cleanup scope agree;
- old plan prompts are absent;
- transition-only documents are identified with an owning deletion task; and
- relative documentation links resolve.

## 3. Automated MIDI gates

### Import

- Golden SMF 0 and 1 fixtures parse deterministically.
- Unsupported format/division/maps fail with stable actionable errors.
- Note pairing, velocity-zero note-off, pickup timing, controllers, and end
  boundaries have regression coverage.
- Source bytes and digest remain unchanged.
- Property/fuzz tests cannot escape the project root or crash on bounded
  malformed inputs.

### Authority

- Fixed tempo/meter/key serialize and reopen unchanged.
- Occurrence timelines are ordered, contiguous, and tick-exact.
- Chord windows cover their intended scope with no inferred substitution.
- Valid chromatic chords remain authoritative.
- Authority changes invalidate only dependent candidates/exports.

### Generation

- Same inputs/version/seed produce semantically identical events.
- Every event remains inside its role range and occurrence boundary.
- Chord/bass notes satisfy the authoritative chord-window policy.
- Drum pitches and channel follow the percussion contract.
- Complete groove variants are stable; density does not delete arbitrary hits.
- Rejection cannot become an accepted candidate.
- Regeneration never changes or overwrites an existing candidate artifact.

### Persistence and export

- Interrupted writes preserve the last known-good project.
- Artifact paths are confined and digests verified.
- Export snapshot is immutable.
- All MIDI files semantic-reimport successfully.
- Manifest hashes match generated bytes.
- Existing destination is not silently overwritten.
- Complete and role files align at the same song origin/end boundary.

## 4. UI gates

- Only Project, MIDI, Structure & Harmony, Arrange, Review, and Export are
  top-level destinations.
- Every disabled primary action explains its blocker.
- Long import/generation/export work does not block the Compose event thread.
- Cancellation leaves project state valid.
- Keyboard/accessibility semantics cover navigation, transport, candidate
  selection, approval, regeneration, and export.
- Restart restores persisted authority and acceptance state.
- No removed page is reachable through a hidden route or settings entry.

## 5. Audition gates

- Play, pause, stop, seek, loop, mute, and solo behave predictably.
- Repeated start/stop does not leak devices, threads, or stuck notes.
- Candidate comparison uses the same timing origin and authority context.
- Device absence or loss is recoverable and cannot mutate the project.
- Audition does not write WAV/MP3 or require the old renderer/sound library.

## 6. DAW gates

Run the complete matrix in `DAW_COMPATIBILITY.md` whenever export semantics
change.

Mandatory release result:

- Logic Pro: pass or documented conditional pass for complete and role files.
- No timing, note, channel, stuck-note, role-separation, or file-open failure.
- Any required tempo-import action is shown in the Export UI and docs.

Record exact DAW/macOS versions; “supports MIDI” is not sufficient evidence.

## 7. Musical evaluation set

### Development set

Use three fixtures while developing:

- simple diatonic 4/4 melody;
- pickup and sub-bar chord-change melody; and
- chromatic/expressive melody with supported controllers.

They prevent regression but are not production acceptance evidence.

### Holdout set

Use at least ten unseen MIDI projects that were not tuned into generator
constants. Include different keys, tempos, section orders, melody registers,
rhythmic activity, sustained material, repeated sections, and chromatic harmony.

Each project is evaluated without changing the authoritative melody or harmony
to make the generator look better.

## 8. Musical rubric

Score 1–5 for:

- melody preservation;
- chord/keys support and space;
- bass harmonic correctness and motion;
- drum groove completeness and section energy;
- role interaction/collision control;
- section development and transitions;
- usefulness of alternatives; and
- overall readiness for DAW continuation.

Mandatory threshold:

- melody preservation is 5 for every project;
- no core role score is below 3;
- no overall project score is below 3;
- median overall score is at least 4;
- no blocker requires editing generated MIDI outside Melotrail merely to align
  timing/harmony; and
- median review time from first generation to accepted arrangement is no more
  than 15 minutes, excluding authority entry and DAW sound selection.

A score includes a short reason and candidate/export snapshot IDs. An average
cannot hide a failed project.

## 9. Cleanup gate

After cutover:

- no Python production/test source or requirements remain;
- no worker process/client/configuration remains;
- no audio import, transcription, DSP, rendering, mixing, mastering, sound
  library, video, publishing, or commercial-release runtime remains;
- no old schema/stage compatibility branch remains;
- no obsolete UI route remains;
- no repository-owned old audio project or bundled rejected-product media
  remains;
- build scripts and docs contain no active worker/audio instructions;
- transition documents and fixtures have been removed with their owners; and
- a clean checkout passes `make test` and `make build`.

## 10. Release evidence

The final MVP evidence bundle contains:

- clean build/test output;
- automated fixture summary;
- holdout rubric with hashes;
- Logic Pro compatibility record;
- UI acceptance result;
- cleanup scan/result;
- known limitations; and
- user sign-off.

The product is not called production-ready until all mandatory evidence exists.
